runtime: nodejs20
instance_class: F2

env_variables:
  NODE_ENV: 'production'

handlers:
  - url: /static
    static_dir: build/static/
  - url: /
    static_files: build/index.html
    upload: build/index.html
